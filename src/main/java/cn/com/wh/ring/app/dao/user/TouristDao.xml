<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.wh.ring.app.dao.user.TouristDao">
    <resultMap id="touristResultMap" type="TouristPojo">
        <id property="id" column="ID" />
        <result property="terminalMark" column="TERMINAL_MARK" />
        <result property="osType" column="OS_TYPE" />
        <result property="count" column="COUNT" />
        <result property="lastTime" column="LAST_TIME" jdbcType="TIMESTAMP"/>
        <result property="createTime" column="CREATE_TIME" jdbcType="TIMESTAMP"/>
        <result property="state" column="STATE"/>
    </resultMap>

    <insert id="insertOrUpdate" parameterType="TouristPojo" useGeneratedKeys="true" keyColumn="id">
        INSERT INTO tourist (TERMINAL_MARK, OS_TYPE, LAST_TIME, COUNT, CREATE_TIME)
        VALUES (#{terminalMark}, #{osType}, NOW(), COUNT+1, NOW())
        ON DUPLICATE KEY  UPDATE LAST_TIME = NOW(), COUNT = COUNT + 1
    </insert>

    <update id="updateState" >
        UPDATE tourist
        SET STATE = #{state}
        WHERE TERMINAL_MARK = #{terminalMark}
    </update>

    <select id="queryByTerminalMark" resultMap="touristResultMap">
        SELECT * FROM tourist WHERE TERMINAL_MARK = #{terminalMark}
    </select>
</mapper>