<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.wh.ring.app.dao.praise.PraiseDao">
    <resultMap id="praiseResultMap" type="PraisePojo">
        <id property="id" column="ID"/>
        <result property="postId" column="POST_ID"/>
        <result property="mark" column="MARK"/>
        <result property="isBad" column="IS_BAD"/>
        <result property="createTime" column="CREATE_TIME" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insert" parameterType="PraisePojo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO praise (POST_ID, MARK, CREATE_TIME) VALUES (#{postId}, #{mark}, NOW())
    </insert>

    <select id="query" resultMap="praiseResultMap">
        SELECT * FROM praise WHERE POST_ID = #{postId} AND MARK = #{mark}
    </select>
</mapper>